
package framework.ui;

import javax.microedition.lcdui.TextField;

public class Editor extends TextField {

    public Editor(String label, String text, int maxSize, int constraints) {
        super(label, text, maxSize, constraints);
    }
}
